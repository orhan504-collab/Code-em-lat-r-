include ':app'

def localPropertiesFile = new File(rootProject.projectDir, "local.properties")
def properties = new Properties()

assert localPropertiesFile.exists(), "local.properties not found"
localPropertiesFile.withInputStream { properties.load(it) }

def flutterRoot = properties.getProperty("flutter.sdk")
assert flutterRoot != null, "flutter.sdk not set in local.properties"

apply from: "$flutterRoot/packages/flutter_tools/gradle/app_plugin_loader.gradle"
