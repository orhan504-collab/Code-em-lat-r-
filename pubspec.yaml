name: meta_app_factory
description: "Kendi kendini kodlayan mobil fabrika."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Yapay zeka beyin (Gemini API)
  google_generative_ai: ^0.4.0
  # Dosya sistemine erişim
  path_provider: ^2.1.2
  # Dosya ve klasör yönetimi için
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
